/* 萌宠/萌娃墙样式 */

.page {
  min-height: 100vh;
  background: var(--bg);
}

/* 顶部切换 */
.tabs {
  display: flex;
  background: #fff;
  padding: var(--spacing-lg);
  gap: var(--spacing-md);
  position: sticky;
  top: 0;
  z-index: 100;
  box-shadow: var(--shadow-sm);
}

.tab {
  flex: 1;
  text-align: center;
  padding: 20rpx 0;
  font-size: var(--text-body);
  color: var(--text-light);
  border-radius: var(--radius-lg);
  transition: all 0.3s ease;
  background: var(--bg);
}

.tab.active {
  background: linear-gradient(135deg, var(--accent), #9AE3D6);
  color: #fff;
  font-weight: 600;
}

/* 档案滚动 */
.profiles {
  background: #fff;
  padding: var(--spacing-lg) 0;
  white-space: nowrap;
}

.profile-scroll {
  padding: 0 var(--spacing-lg);
  display: flex;
  gap: 20rpx;
}

.profile-card {
  display: inline-flex;
  flex-direction: column;
  align-items: center;
  width: 180rpx;
  background: var(--bg);
  border-radius: var(--radius-lg);
  padding: 20rpx;
  flex-shrink: 0;
}

.profile-avatar {
  width: 120rpx;
  height: 120rpx;
  border-radius: var(--radius-circle);
  margin-bottom: var(--spacing-sm);
  border: 4rpx solid #fff;
  box-shadow: var(--shadow-sm);
}

.profile-name {
  font-size: var(--text-secondary);
  font-weight: 600;
  color: var(--text);
  margin-bottom: var(--spacing-xs);
}

.profile-stats {
  display: flex;
  gap: var(--spacing-sm);
  font-size: var(--text-tiny);
  color: var(--text-muted);
}

.profile-badges {
  margin-top: var(--spacing-xs);
}

.badge {
  font-size: 20rpx;
  padding: 4rpx 10rpx;
  background: linear-gradient(135deg, var(--accent), #9AE3D6);
  color: #fff;
  border-radius: var(--radius-sm);
}

/* 添加卡片 */
.add-card {
  justify-content: center;
  border: 2rpx dashed var(--accent);
  background: transparent;
}

.add-icon {
  font-size: 48rpx;
  color: var(--accent);
  margin-bottom: 4rpx;
  line-height: 1;
}

.add-text {
  font-size: var(--text-tiny);
  color: var(--accent);
}

/* 时光轴 */
.timeline {
  padding: var(--spacing-lg);
}

.timeline-header {
  margin-bottom: 20rpx;
}

.timeline-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text);
}

.timeline-list {
  display: flex;
  flex-direction: column;
  gap: 20rpx;
}

/* 瞬间卡片 */
.moment-card {
  background: #fff;
  border-radius: var(--radius-lg);
  padding: var(--spacing-lg);
  box-shadow: var(--shadow-sm);
}

.moment-header {
  display: flex;
  align-items: center;
  margin-bottom: var(--spacing-md);
}

.moment-avatar {
  width: 64rpx;
  height: 64rpx;
  border-radius: var(--radius-circle);
  margin-right: var(--spacing-sm);
}

.moment-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.moment-name {
  font-size: var(--text-secondary);
  font-weight: 600;
  color: var(--text);
}

.moment-time {
  font-size: var(--text-tiny);
  color: var(--text-muted);
}

.moment-age {
  font-size: var(--text-tiny);
  padding: 6rpx 12rpx;
  background: var(--bg);
  color: var(--accent);
  border-radius: var(--radius-sm);
}

/* 内容 */
.moment-content {
  font-size: var(--text-body);
  color: var(--text);
  line-height: 1.6;
  margin-bottom: var(--spacing-md);
}

/* 图片 */
.moment-images {
  border-radius: var(--radius-md);
  overflow: hidden;
  margin-bottom: var(--spacing-md);
}

.moment-img {
  width: 100%;
  height: 400rpx;
}

.moment-images.grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 8rpx;
}

.moment-images.grid .moment-img {
  height: 280rpx;
}

/* 互动 */
.moment-actions {
  display: flex;
  gap: var(--spacing-xl);
  padding-top: var(--spacing-md);
  border-top: 1rpx solid var(--border);
}

.action-btn {
  display: flex;
  align-items: center;
  gap: var(--spacing-xs);
}

.action-icon {
  font-size: 32rpx;
  line-height: 1;
}

.action-count {
  font-size: var(--text-small);
  color: var(--text-muted);
}

/* 底部占位 */
.tabbar-spacer {
  height: 120rpx;
}